import { SHOW_LOADING } from '../Actions/types';

const INITIAL_STATE = {isLoading:false }

export default genericReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}